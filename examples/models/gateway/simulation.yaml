# Copyright 2025 Robert Bosch GmbH
#
# SPDX-License-Identifier: Apache-2.0

---
kind: Stack
metadata:
  name: remote
spec:
  connection:
    transport:
      redis:
        uri: redis://localhost:6379
        timeout: 60
  models:
    - name: gateway
      uid: 42
      model:
        name: Gateway
      channels:
        - name: signal
          alias: signal
          selectors:
            channel: signal
---
kind: Model
metadata:
  name: simbus
