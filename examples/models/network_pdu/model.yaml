# Copyright 2024 Robert Bosch GmbH
#
# SPDX-License-Identifier: Apache-2.0

---
kind: Model
metadata:
  name: Network_PDU
spec:
  runtime:
    dynlib:
      - os: linux
        arch: amd64
        path: lib/libnetwork_pdu.so
      - os: linux
        arch: x86
        path: lib/libnetwork_pdu.so
      - os: windows
        arch: x64
        path: lib/libnetwork_pdu.dll
      - os: windows
        arch: x86
        path: lib/libnetwork_pdu.dll
  channels:
    - alias: binary
      selectors:
        channel: binary_channel
