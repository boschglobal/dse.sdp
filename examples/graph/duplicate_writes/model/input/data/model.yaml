---
kind: Model
metadata:
  name: Csv
spec:
  runtime:
    dynlib:
      - os: linux
        arch: amd64
        path: model/input/lib/libcsv.so
  channels:
    - alias: scalar
      selectors:
        channel: scalar_channel
