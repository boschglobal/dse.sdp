{
    "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
    "name": "dse",
    "scopeName": "source.dse",
    "patterns": [{
            "include": "#keywords"
        },
        {
            "include": "#strings"
        }
    ],
    "repository": {
        "keywords": {
            "patterns": [{
                "name": "keyword.control.dse",
                "match": "\\b(simulation|env|model|channel|uses|workflow|from|as|stack|network|var|arch|file|uid|envar)\\b",
                "scope": "keyword.control"
            }]
        }
    }
}
